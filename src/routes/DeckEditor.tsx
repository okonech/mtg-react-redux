import React from 'react';
import GamePage from '../components/GamePage';

const DeckEditor = () => {
    return (
        <GamePage
            playerRows={1}
            playerCols={1}
            playersNum={1}
        />
    );
};

export default DeckEditor;
