---
name: Card
---

import { Playground, Props } from 'docz'
import Card from '../../src/components/Card'
import HoverCard from '../../src/util/docz-helpers/HoverCard'
import { exampleCardModel } from '../../src/util/docz-helpers/card'
import CssBaseline from '@material-ui/core/CssBaseline';
import { createMuiTheme, MuiThemeProvider } from '@material-ui/core/styles';

# Card

<Props of={Card} />


## Basic usage

<Playground>
  <MuiThemeProvider theme={createMuiTheme({
        palette: {
            primary: {
            main: '#1e88e5'
            },
            secondary: {
            main: '#d81b60'
            },
            type: 'dark'
        }})}>
      <CssBaseline />
      <Card
            card={exampleCardModel()}
            selected={false}
            selecting={false}
            cardHeight={32}
            isHovered={false}
        />
    </MuiThemeProvider>
</Playground>

## Selected

<Playground>
  <MuiThemeProvider theme={createMuiTheme({
        palette: {
            primary: {
            main: '#1e88e5'
            },
            secondary: {
            main: '#d81b60'
            },
            type: 'dark'
        }})}>
      <CssBaseline />
      <Card
            card={exampleCardModel()}
            selected={true}
            selecting={false}
            cardHeight={32}
            isHovered={false}
        />
    </MuiThemeProvider>
</Playground>

## With Top Label

<Playground>
  <MuiThemeProvider theme={createMuiTheme({
        palette: {
            primary: {
            main: '#1e88e5'
            },
            secondary: {
            main: '#d81b60'
            },
            type: 'dark'
        }})}>
      <CssBaseline />
      <HoverCard
            card={exampleCardModel()}
            selected={false}
            selecting={false}
            cardHeight={32}
            topLabel={'Sample Top Label'}
        />
    </MuiThemeProvider>
</Playground>

## With Hover

<Playground>
  <MuiThemeProvider theme={createMuiTheme({
        palette: {
            primary: {
            main: '#1e88e5'
            },
            secondary: {
            main: '#d81b60'
            },
            type: 'dark'
        }})}>
      <CssBaseline />
      <HoverCard
            card={exampleCardModel()}
            selected={false}
            selecting={false}
            cardHeight={32}
        />
    </MuiThemeProvider>
</Playground>